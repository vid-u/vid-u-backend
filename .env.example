NODE_ENV=development
PORT=3001

DATABASE_URL="postgresql://postgres:postgres@localhost:5434/vidu_waitlist?schema=public"

# Browser origins allowed to call this API (CORS), comma-separated.
# Production: include every hostname users hit (e.g. https://www.vid-u.com,https://vid-u.com).
# The server also allows the matching apex/www pair for each HTTPS origin.
FRONTEND_URL=http://localhost:5173
